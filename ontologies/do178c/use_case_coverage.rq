PREFIX coua: <https://gitlab.dlr.de/ft-ssy-avs/ap/coua/schema#>

SELECT ?uc ((COUNT(distinct ?req) > 0) AS ?covered)
WHERE
{
	?req coua:useCase ?uc ;
		a coua:Requirement ;
		coua:description ?desc .
	?uc a coua:UseCase ;
		coua:story ?story .
}
GROUP BY ?uc
